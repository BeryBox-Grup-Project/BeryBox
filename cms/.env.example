# Leave VITE_API_URL empty in local dev so Vite proxies /login and /admin
# to the Express server and login is not blocked by CORS.
# VITE_API_URL=http://localhost:3000
VITE_CLIENT_URL=http://localhost:5173
